# UTM Borrow — backend dependencies.
# Version floors chosen so the stack installs cleanly on current Python (3.11–3.14)
# with prebuilt wheels (no Rust toolchain needed). Same libraries as the documented
# stack (SDD §1.4): FastAPI + Motor (async MongoDB) + JWT/bcrypt.
fastapi>=0.115
uvicorn>=0.30
motor>=3.5
pymongo>=4.8
python-dotenv>=1.0
bcrypt>=4.1
PyJWT>=2.8
pydantic>=2.9
email-validator>=2.1
python-multipart>=0.0.9
